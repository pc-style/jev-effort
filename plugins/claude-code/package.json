{
  "name": "@jev-effort/claude-code",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/hook.ts --target=node --format=esm --minify --outfile dist/hook.mjs"
  },
  "dependencies": {
    "@jev-effort/core": "workspace:*"
  }
}
