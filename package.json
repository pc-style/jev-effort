{
  "name": "jev-effort",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "plugins/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "classify": "bun packages/core/src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7"
  }
}
